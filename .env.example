# Scale Daemon Security Configuration
# Copy this to .env and fill in real values
SCALE_DASHBOARD_PASSWORD=scale
SCALE_AUTH_TOKEN=scale-token
SCALE_PORT=8765
TICKET_DASHBOARD_PASSWORD=ticket
TICKET_AUTH_TOKEN=ticket-token
TICKET_PORT=8766